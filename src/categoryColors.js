// Fixed hue per category (identity, not rank) — shared by CategoryChart and
// TransactionList's stamp chips so the same category always reads as the same ink.
export const CATEGORY_COLORS = {
  food: '#2a78d6',
  housing: '#008300',
  utilities: '#e87ba4',
  transport: '#eda100',
  entertainment: '#1baf7a',
  salary: '#eb6834',
  other: '#4a3aa7',
};

export const FALLBACK_CATEGORY_COLOR = '#6e685c';
